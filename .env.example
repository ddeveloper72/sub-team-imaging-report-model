# Environment Variables for Production Deployment
# ===============================================
# Copy this file to .env and update values for local development
# For production (Heroku/Railway/Render), set these in platform dashboard

# REQUIRED VARIABLES
# ------------------

# Flask Security (CRITICAL - Generate a strong secret key)
SECRET_KEY=your-secret-key-here-generate-a-strong-64-character-string

# Environment Configuration
FLASK_ENV=production
DEBUG=False

# Server Configuration (Platform usually handles these)
HOST=0.0.0.0
PORT=5000

# OPTIONAL VARIABLES
# ------------------

# Application Paths
BASE_DIR=/app

# PDF Generation Settings
PDF_TIMEOUT=30
MAX_PDF_SIZE=50MB

# Performance Settings
WEB_CONCURRENCY=2
MAX_WORKERS=4

# Security Settings
SESSION_COOKIE_SECURE=True
SESSION_COOKIE_HTTPONLY=True

# Logging
LOG_LEVEL=INFO
REQUEST_LOGGING=True

# PLATFORM-SPECIFIC NOTES
# ------------------------
# Heroku: PORT is automatically set by the platform
# Railway: All variables can be set in the dashboard
# Render: Set in environment variables section
# 
# SECRET_KEY Generation:
# python -c "import secrets; print(secrets.token_hex(32))"